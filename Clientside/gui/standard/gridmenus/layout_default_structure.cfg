ask selection 0
ask objecttype #answer#
objloop INCLUDE 1 NOWRAP FORWARD name #answer# "~
	objput commandCenter _temp"

set grid0 TECH
set grid1 TECH
set grid2 TECH
set grid3 TECH

set grid4 TECH
set grid5 TECH
set grid6 TECH
set grid7 TECH

set grid8 TECH
set grid9 TECH

ask stringsmatch #answer# beast_entropy_shrine
if [answer] set grid10 GATEWAY
if [!answer] set grid10 TECH

if [_temp] set grid11 TECH
if [!_temp] set grid11 DEMOLISH

ask selection 0
ask objecttype #answer#
ask stringsmatch #answer# human_arrow_tower
if [answer] set grid3 ATTACK

ask selection 0
ask objecttype #answer#
ask stringsmatch #answer# human_chemical_tower
if [answer] set grid3 ATTACK

ask selection 0
ask objecttype #answer#
ask stringsmatch #answer# beast_spire
if [answer] set grid3 ATTACK

ask selection 0
ask objecttype #answer#
ask stringsmatch #answer# beast_fire_spire
if [answer] set grid3 ATTACK

ask selection 0
ask objecttype #answer#
ask stringsmatch #answer# human_electric_tower
if [answer] set grid3 ATTACK

ask selection 0
ask objecttype #answer#
ask stringsmatch #answer# beast_entropy_spire
if [answer] set grid3 ATTACK

ask selection 0
ask objecttype #answer#
ask stringsmatch #answer# beast_strata_spire
if [answer] set grid3 ATTACK
